## Summary
- fix the documented launch command to use the actual installed app path at `/Applications/MacRunner.app`
- update the Homebrew cask caveats, README quick start, and local install output so they stay consistent

## Testing
- not run (docs/install text only)
